[project]
name = "makedataset"
version = "0.0"
description = "Make MRI datasets for clinical studies"

[build-system]
requires = ["setuptools >= 61.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]